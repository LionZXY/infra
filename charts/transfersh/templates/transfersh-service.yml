apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfersh.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "transfersh.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.transfersh.service.port }}
      targetPort: {{ .Values.transfersh.service.port }}
